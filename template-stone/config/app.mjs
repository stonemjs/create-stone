export const app = {
  /**
   * The environnement
   * Ex: production, staging, developement, local
   */
  env: 'local',

  /**
   * Debug mode
   */
  debug: false,

  /**
   * Current app locale
   */
  locale: 'en',
  
  /**
   * Fallback locale
   */
  fallbackLocale: 'en',
}